<!DOCTYPE html>

<title>New Institution</title>

{% block body %}
    <h1>Create new Institution</h1>

    {{ include('institution/_form.html.twig') }}

    <a href="{{ path('app_institution_index') }}">back to list</a>
{% endblock %}
