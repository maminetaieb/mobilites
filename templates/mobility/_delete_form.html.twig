<form method="post" action="{{ path('app_mobility_delete', {'id': mobility.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mobility.id) }}">
    <button class="btn">Delete</button>
</form>
